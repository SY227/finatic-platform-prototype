# Finatic platform prototype - local run steps

# IMPORTANT: run these from the project folder, not from ~
cd ~/Desktop/finatic-platform-prototype

# Use pnpm for this repo (lockfile is pnpm-lock.yaml)
pnpm install
pnpm build
pnpm dev

# App URL
# http://localhost:3000
